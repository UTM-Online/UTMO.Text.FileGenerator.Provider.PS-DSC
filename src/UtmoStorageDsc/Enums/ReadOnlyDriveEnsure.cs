namespace UTMO.Text.FileGenerator.Provider.DSC.UtmoStorage;

/// <summary>
/// Specifies the ensure state for the xReadOnlyDrive DSC resource.
/// </summary>
public enum ReadOnlyDriveEnsure
{
    /// <summary>
    /// The disk that owns the specified drive letter is read-only.
    /// </summary>
    ReadOnly,
}
